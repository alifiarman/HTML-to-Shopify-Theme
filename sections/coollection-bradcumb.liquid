
    <div class="container">
       <div class="row">
          <div class="col-xxl-12">
             <div class="breadcrumb__content p-relative z-index-1">
                {% assign title = collection.title | downcase %}

                <h3 class="breadcrumb__title">
                    {% if title == 'products' %}
                         All {{ collection.title }}
                    {% else %}
                        {{ collection.title }}
                    {% endif %}
                    
                </h3>
                <div class="breadcrumb__list">
                   <span><a href="{{ shop.url }}">Home</a></span>
                   <span>
                    {% if title == 'products' %}
                        All {{ collection.title }}
                    {% else %}
                        {{ collection.title }}
                    {% endif %}
                    </span>
                </div>
             </div>
          </div>
       </div>
    </div>



{% schema %}
    {
        "name": "coollection-bradcumb",
        "class": "breadcrumb__area include-bg pt-100 pb-50",
        "settings": [

        ]
    }
{% endschema %}